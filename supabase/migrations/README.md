# Supabase migrations — intentionally empty

The database schema for this project is owned by **Alembic**
(`backend/alembic/versions/`), applied via `alembic upgrade head` against the
Supabase connection. Do **not** add SQL migrations here unless you are
deliberately migrating off Alembic — running both will cause schema conflicts.
